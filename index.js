'use strict';

const typescript = require('./configurations/typescript');
const mocha = require('./configurations/mocha');

module.exports = {
    configs: {
        typescript,
        mocha
    }
};